
@{
    ViewBag.Title = "登陆页";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>LayuiAdmin登录</title>
    @*<link rel="stylesheet" href="../plug-in/layui/css/layui.css">*@
    <style>
        .card {
            width: 400px;
            height: 200px;
            position: absolute;
            top: 30%;
            left: 37%;
            padding-top: 50px;
            background: black;
            /* opacity: 0.4; */
            background: rgba(0, 0, 0, 0.27);
            border-radius: 4px;
        }

        .layui-form-item {
            opacity: 1.0;
        }

        body {
            background: url("ReSources/img/background1.jpg");
        }

        .iconiii {
            font-size: 16px;
            color: #777;
            position: relative;
            margin-left: 100px;
            z-index: 100;
        }
    </style>

</head>
<body class="layui-layout-body">
    <div class="card">
        <form class="layui-form" action="index.html">
            <div class="layui-form-item">
                <label class="layui-form-label"><i class="layui-icon layui-icon-username iconiii"></i> </label>
                <div class="layui-input-inline">
                    <input type="text" name="userid" id="uid"
                           required lay-verify="required" lay-verType="tips"
                           placeholder="请输入账号" autocomplete="off" class="layui-input"
                           style="padding-left: 25px; border-radius: 4px;">
                </div>
            </div>
            <div class="layui-form-item">
                <label class="layui-form-label"><i class="layui-icon layui-icon-password iconiii"></i> </label>
                <div class="layui-input-inline">
                    <input type="password" name="password" id="pwd"
                           required lay-verify="required" lay-verType="tips"
                           placeholder="请输入密码" autocomplete="off" class="layui-input" style="padding-left: 25px; border-radius: 4px;">
                </div>
            </div>
            <div class="layui-form-item">
                <div class="layui-input-block">
                    <button class="layui-btn" lay-submit lay-filter="Formlogin" style="width:190px;border-radius: 4px;">登录</button>
                </div>
            </div>
        </form>
    </div>

    <!--js引用-->
    <!--插件引用-->
    @*<script src="../plug-in/layui/layui.js"></script>*@

    <!--其他js引用-->
    @*<script src="config/config.js"></script>
    <script src="config/public.js"></script>*@
    @*<script src="login.js"></script>*@
    <script src="~/ReSources/js/md5.js"></script>
     <script type="text/javascript">

         layui.use(['form', 'layer', 'jquery'], function () {
             var $ = layui.$, layer = layui.layer, form = layui.form;

             //监听提交
             form.on('submit(Formlogin)', function (data) {
                 // layer.msg(JSON.stringify(data.field));
                 var user = data.field;


                 //if (user.userid != "admin") {
                 //    layer.tips('用户名错误', '#uid', {
                 //        tips: 2
                 //    });
                 //    return false;
                 //}
                 //if (user.password != '123456') {
                 //    layer.tips('密码错误', '#pwd', {
                 //        tips: 2
                 //    });
                 //}
                 //if (user.userid == "admin" && user.password == '123456') {
                     /*浏览器窗口存储*/
                     user.img = "img/head.jpg";/*头像*/
                     user.username = '王东旭';
                     //websessionStorage.setItem('user', user);
                     window.location.href ='Home/index';
                     
                 //}

                 /*ajax请求*/
                 // var url='../jsondata/tableJson.json';
                 // ax.axget(url, user,function (json) {
                 //     if(json.code==0){
                 /*浏览器窗口存储*/
                 //      websessionStorage.setItem('user',user);
                 //         window.location.href = "index.html";
                 //     }
                 // });
                 return false;
             });
         });
     </script>
</body>
</html>
